import { TokenType, Token } from "./tokens";

export default class Scanner {
  constructor(source: string) {

  }

  addTokens(): Array<Token> {
    const tempToken: Array<Token> = []
    return tempToken
  }
}